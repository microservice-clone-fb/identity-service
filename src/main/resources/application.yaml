server:
  port: ${PORT:5001}
  servlet:
    context-path: ${SERVER_SERVLET_CONTEXT_PATH:/identity}

spring:
  application:
    name: identity-service
  datasource:
    url: ${DB_URL:jdbc:postgresql://dpg-d45e3sv5r7bs73ag0opg-a.singapore-postgres.render.com:5432/clone_fb_identity}
    driverClassName: ${DB_DRIVER:org.postgresql.Driver}
    username: ${DB_USERNAME:user}
    password: ${DB_PASSWORD:V1pI4T1SSv62ioLNFwIXNwyfhPpKeS2Hm}
  jpa:
    hibernate:
      ddl-auto: ${DB_DDL_AUTO:update}
    show-sql: ${DB_SHOW_SQL:true}

  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9000}
    producer:
      key-serializer: ${KAFKA_KEY_SERIALIZER:org.apache.kafka.common.serialization.StringSerializer}
      value-serializer: ${KAFKA_VALUE_SERIALIZER:org.springframework.kafka.support.serializer.JsonSerializer}

jwt:
  signerKey: ${JWT_SIGNER_KEY:1TjXchw5FloESb63Kc+DFhTARvpWL4jUGCwfGWxuG5SIf/1y/LgJxHnMqaF6A/ij}
  valid-duration: ${JWT_VALID_DURATION:1800}
  refreshable-duration: ${JWT_REFRESHABLE_DURATION:604800}

app:
  services:
    profile: ${PROFILE_SERVICE_URL:http://localhost:5002/profile}
    file: ${FILE_SERVICE_URL:http://localhost:5005/file}